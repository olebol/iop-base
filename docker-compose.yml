services:
  iris:
    build: .
    restart: always
    # environment:
      # - ISC_CPF_MERGE_FILE=/irisdev/app/merge.cpf
    ports:
      - 1972:1972
      - 52773:52773
    # volumes:
      # - .:/irisdev/app